/* pg_fts--1.0.4--1.0.5.sql */
-- Bug-fix release (build-memory flush-budget accounting).  No catalog or
-- on-disk format change from 1.0.4; this upgrade is intentionally a no-op
-- (ALTER EXTENSION pg_fts UPDATE TO '1.0.5').
