/* pg_fts--1.0.1--1.0.2.sql */
-- Bug-fix release (positions-decode allocation guard).  No catalog or on-disk
-- format change from 1.0.1; this upgrade is intentionally a no-op
-- (ALTER EXTENSION pg_fts UPDATE TO '1.0.2').
