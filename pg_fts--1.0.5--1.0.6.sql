/* pg_fts--1.0.5--1.0.6.sql */
-- Bug-fix release (build flush-budget cap + post-build truncate).  No catalog
-- or on-disk format change from 1.0.5; this upgrade is intentionally a no-op
-- (ALTER EXTENSION pg_fts UPDATE TO '1.0.6').
