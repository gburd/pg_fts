/* pg_fts--1.0.0--1.0.1.sql */
-- Bug-fix release (streaming merge / index-build memory bound).  No catalog or
-- on-disk format change from 1.0.0; this upgrade is intentionally a no-op
-- (ALTER EXTENSION pg_fts UPDATE TO '1.0.1').
