# Build artifacts
*.o
*.so
*.bc
*.dylib
*.dll
pg_fts.so

# PGXS / regression output
/results/
/output_iso/
regression.diffs
regression.out
/log/
/tmp_check/
/tmp_check_iso/
.deps/

# editor / OS
*~
*.swp
.DS_Store

# other
.memelord

# agent tooling (not part of the extension)
.claude/
.mcp.json
AGENTS.md
.agent/
.kiro/
.pi/


# make dist source-distribution artifact
pg_fts-*.zip

# rendered docs (built by `make html` / `nix run .#docs-html`; published to
# GitHub/Codeberg Pages by CI, never committed to main)
/doc/html/
/build/
